import { Canvas, CanvasKit } from "canvaskit-wasm";

// Exports the current canvas to a PDF file using Skia's PDF backend.
function exportToPdf(canvasKit: CanvasKit, canvas: Canvas, filename: string, pageWidth: number, pageHeight: number): void {
}
